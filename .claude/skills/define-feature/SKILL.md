---
description: "Define Feature — grills the user to define a new feature, then writes feature.md in docs/features/FTR-XXX-slug/. Usage: /define-feature"
disable-model-invocation: true
---

Spawn the `define-feature` agent with no additional arguments:

```
subagent_type: define-feature
prompt: Start the feature definition session.
```

Do not add anything else. The agent handles discovery, grilling, and file creation autonomously.
